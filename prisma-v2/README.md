# Prisma V2 (placeholder Fase 1)

El schema y migraciones de `ConversationLock`, `Operation`, outbox, etc. se agregan en **Fase 2**.

No usar `DATABASE_URL` de producción. Solo la DB local de `docker-compose.wara-v2.yml`.
