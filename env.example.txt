# Copiar a .env local. En Vercel: Settings → Environment Variables.
# Necesitás al menos UNA pareja completa (email + password): Wara y/o admin.

SESSION_PASSWORD=

PANEL_USER_WARA_EMAIL=
PANEL_USER_WARA_PASSWORD=

PANEL_USER_ADMIN_EMAIL=
PANEL_USER_ADMIN_PASSWORD=

# BuilderBot (prompt + mensajería)
BUILDERBOT_API_URL=https://app.builderbot.cloud
BUILDERBOT_BOT_ID=
BUILDERBOT_API_KEY=
# Secreto tipo Pulze (BuilderBot HTTP: header x-api-key). Podés usar solo una:
# Si un panel trunca la contraseña a 64 caracteres, usá: openssl rand -hex 32 (64 hex).
PULZE_API_KEY=
# Alias del mismo concepto (si ya lo tenías nombrado así)
BUILDERBOT_CONTEXT_API_KEY=
# Opcional pero recomendado para editar el prompt del flow ChatPDF
BUILDERBOT_ANSWER_ID=

# Wara / Visionblo API (api_interna)
# Staging:    https://wara-1470.staging.visionblo.com/rb/app/api_interna
# Producción: https://apps.visionblo.com/rb/app/api_interna
# Endpoint usado: ObtenerContactosPorNumero (token "ATILIO")
WARA_API_BASE_URL=
WARA_OBTENER_EMPRESA_TOKEN=

# Endpoints de mantenimiento: CreateSessionToken, ConsultarEstadoUnidades,
# RegistrarCambioOdometroHorometro. Si no se define, usa WARA_API_BASE_URL.
# Staging probado: https://wara-1470.staging.visionblo.com/rb/app/api_interna
# Producción:      https://apps.visionblo.com/rb/app/api_interna
WARA_MAINTENANCE_API_BASE_URL=

# Fallback opcional para crear SessionToken si ObtenerContactosPorNumero no lo devuelve.
# Para los casos de 1 empresa, Wara ya devuelve SessionToken y estas credenciales no hacen falta.
WARA_SESSION_USER=
WARA_SESSION_PASSWORD=
WARA_SESSION_MAX_IDLE=15m

# Modo whitelist de prueba: solo estos números reciben respuesta del bot.
# Lista separada por comas. Vacío = abierto a todo cliente validado por Wara.
# Ejemplo: WARA_TEST_ALLOWED_PHONES=+5492613867127,+5492612478856
WARA_TEST_ALLOWED_PHONES=
